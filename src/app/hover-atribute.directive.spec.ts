import { HoverAtributeDirective } from './hover-atribute.directive';

describe('HoverAtributeDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverAtributeDirective();
    expect(directive).toBeTruthy();
  });
});
